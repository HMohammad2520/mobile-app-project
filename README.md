# 📱 Modern Mobile Applications

- **دانشگاه**: آزاد واحد قدس
- **دانشکده**: اندیشه
- **درس**: برنامه نویسی موبایل 2
- **استاد**: دکتر آرتین زمردیان
- **دانشجویان**:
   - مهندس نیما ماهر خامنه
   - مهندس محمد حیدری
   - مهندس سروش بهمنی
   - مهندس علی مالمیر

## 📢 مقدمه

این مخزن شامل سورس‌کد یک برنامه موبایل‌محور مدرن است که با استفاده از **React، TypeScript و Vite** توسعه داده شده است.
این پروژه به‌عنوان بخشی از **پروژه دانشگاهی** طراحی و پیاده‌سازی شده و هدف آن نمایش استفاده از تکنولوژی‌های جدید فرانت‌اند برای ساخت اپلیکیشن‌های سریع، مقیاس‌پذیر و واکنش‌گرا است.

---

## 🔗 لینک تست

شما میتوانید با استفاده از لینک زیر نسخه **Web** برنامه را تست کنید.


[Play](https://airalfacyber.netlify.app/)

---

## ☰ فهرست مطالب

* معرفی پروژه
* ویژگی‌ها
* تکنولوژی‌های استفاده‌شده
* راه‌اندازی پروژه
* دستورات موجود
* متغیرهای محیطی
* ساختار پروژه
* استقرار (Deployment)

---

## ⭐ معرفی پروژه

این برنامه با هدف پیاده‌سازی یک اپلیکیشن موبایل‌پسند و مدرن توسعه داده شده است.
در این پروژه سعی شده از معماری تمیز، کامپوننت‌های قابل استفاده مجدد و TypeScript برای افزایش کیفیت و پایداری کد استفاده شود.

این مخزن شامل تمام فایل‌های موردنیاز برای توسعه، تست و اجرای پروژه است.

---

## ✨ ویژگی‌ها

* رابط کاربری واکنش‌گرا (مناسب موبایل و دسکتاپ)
* استفاده از React و معماری کامپوننت‌محور
* TypeScript برای افزایش ایمنی و خوانایی کد
* استفاده از Vite برای سرعت بالای توسعه
* ساختار ماژولار و قابل توسعه
* امکان اتصال به API (در صورت نیاز)
* آماده برای تبدیل به PWA (در صورت استفاده از Service Worker)

---

## ☰ تکنولوژی‌های استفاده‌شده

| بخش               | تکنولوژی                             |
| ----------------- | ------------------------------------ |
| رابط کاربری       | React                                |
| زبان برنامه‌نویسی | TypeScript                           |
| ابزار Build       | Vite                                 |
| استایل‌دهی        | CSS / سایر ابزارها (در صورت استفاده) |

---

## 🚀 راه‌اندازی پروژه

### 📦 پیش‌نیازها

* نصب Node.js (نسخه 14 یا بالاتر پیشنهاد می‌شود)

### 🔧 نصب و اجرا

ابتدا مخزن را کلون کنید:

```bash
git clone https://github.com/HMohammad2520/mobile-app.git
cd mobile-app
```

سپس وابستگی‌ها را نصب کنید:

```bash
npm install
```

اجرای پروژه در حالت توسعه:

```bash
npm run dev
```

---

## </> دستورات موجود

| دستور             | توضیح                        |
| ----------------- | ---------------------------- |
| `npm run dev`     | اجرای پروژه در حالت توسعه    |
| `npm run build`   | ساخت نسخه نهایی (Production) |
| `npm run preview` | پیش‌نمایش نسخه build شده     |

---

## ⚙️ متغیرهای محیطی

در صورت نیاز به تنظیمات خاص (مانند آدرس API)، یک فایل `.env` در ریشه پروژه بسازید:

```env
VITE_API_BASE_URL=<API Key>
GEMINI_API_KEY=<API Key>
```

پس از تغییر متغیرها، پروژه را مجدداً اجرا کنید.

---

## 🧱 ساختار پروژه

نمای کلی از ساختار پوشه‌ها:

```
mobile-app/
├── public/            # فایل‌های استاتیک
├── src/
│   ├── components/    # کامپوننت‌های قابل استفاده مجدد
│   ├── services/      # منطق برنامه و ارتباط با API
│   ├── App.tsx        # کامپوننت اصلی
│   ├── index.tsx      # نقطه ورود برنامه
│   └── styles/        # فایل‌های استایل
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

---

## 🌐 استقرار (Deployment)

برای انتشار پروژه می‌توانید از سرویس‌های زیر استفاده کنید:

* GitHub Pages
* Netlify
* Vercel
* Firebase Hosting

ساخت نسخه نهایی:

```bash
npm run build
```

سپس محتوای پوشه `dist/` را روی سرویس موردنظر آپلود کنید.
